query ActualTimetables($weekNumber: Int) {
    actualTimetables(where: { weekNumber: { eq: $weekNumber } }) {
        id
        weekNumber
        modifiedAt
        group {
            name
            id
        }
    }
}
